---
alwaysApply: true
---
# Tech Stack
frontend: React Native, Expo, Nativewind

# After making changes
- Do NOT run the formatter
- Do NOT run the tests or add any new tests unless explicitly asked to do so
- Run the linting via `pnpm lint` and fix any linting errors
- Ensure that all type errors are resolved